<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.liyuyouguo.common.mapper.AdminMapper">

    <sql id="Base_Column_List">
        ${alias}.id,
        ${alias}.username,
        ${alias}.password,
        ${alias}.password_salt,
        ${alias}.last_login_ip,
        ${alias}.last_login_time,
        ${alias}.is_delete
    </sql>

</mapper>
